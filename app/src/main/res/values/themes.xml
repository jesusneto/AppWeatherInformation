<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AppWeatherInformation" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="CustomSearchView" parent="Widget.AppCompat.SearchView">
        <item name="android:editTextColor">@color/white</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorHint">@color/white</item>
        <!--        <item name="android:searchIcon">@drawable/ic_search_icon</item>-->
        <!-- Comentei porque não era mais necessário -->
    </style>

    <style name="Theme.AppWeatherInformation" parent="Base.Theme.AppWeatherInformation" />
</resources>